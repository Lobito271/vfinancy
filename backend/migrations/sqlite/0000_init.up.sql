-- 0000_init.up.sql (SQLite)
-- The schema_migrations bookkeeping table is created by the migration
-- runner before this file runs. SQLite needs no extension and has no
-- set_updated_at() trigger function: every table sets its updated_at
-- column explicitly from the application. This migration is therefore
-- a deliberate no-op that documents the difference.

SELECT 1;
